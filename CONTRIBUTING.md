# 贡献指南

感谢你愿意一起完善这个 GPT Image 2 提示词库。

这个仓库的目标不是收集越多越好，而是把真正有复用价值、来源清楚、方便预览的提示词沉淀成长期可用的卡片。

## 可以贡献什么

- 新提示词：适合长期收藏、能生成明确视觉效果的提示词
- 效果图：用仓库内提示词生成的预览图
- 标签建议：合并同义标签、补充更好用的检索标签
- 内容修正：错别字、来源补充、图片路径、提示词说明、生成注意事项
- 体验建议：README、分类、展示站数据、搜索体验等改进

## 推荐贡献方式

如果你只是想分享一个提示词，优先开 Issue：

- 使用 `投稿提示词` 模板
- 附上原始提示词
- 写清楚来源
- 说明它适合什么场景

如果你已经能整理成正式卡片，可以直接开 Pull Request：

- 新增卡片放入 `library/<scene>/`
- 效果图放入 `assets/previews/`
- 更新 `site-data/`：

```bash
npm run export:site-data
npm run validate
```

## 提示词入库标准

一条提示词至少需要满足：

- 适用场景清楚
- 能直接复制使用
- 有明确来源或来源说明
- 标题、摘要、标签能帮助别人快速判断用途
- 如果有外部效果图，必须说明来源，不要直接搬运未授权图片

正式卡片必须包含：

- `title`
- `status`
- `cover`
- `preview`
- `preview_type`
- `preview_source`
- `scene`
- `tags`
- `prompt`
- `summary`
- `source`
- `collected_at`

## 图片与版权规则

- 优先使用自己用当前提示词生成的效果图
- 外部参考图没有明确授权时，不要下载进仓库
- 可以在正文或 Issue 里贴外部参考链接
- 生成图建议命名为 `xxx-generated.png`
- 不满意的迭代图用 `xxx-generated-v2.png`，不要直接覆盖已有可用图

`preview_type` 只使用：

- `generated`：自己用该提示词生成
- `reference`：外部参考链接
- `licensed`：明确授权素材
- `placeholder`：占位图

## 提交前检查

开 PR 前请尽量运行：

```bash
npm test
npm run validate
```

如果你只是在 Issue 里投稿提示词，不需要运行这些命令。

## 内容流转

推荐流程：

1. 先通过 Issue 或 `inbox/` 快速收集
2. 再整理成 `library/` 正式卡片
3. 生成并回填效果图
4. 运行数据导出和校验
5. 必要时再加入 README 精选

## 命名约定

- 目录名和文件名用英文短横线
- 中文说明为主
- 提示词正文保留原语言
- 标签尽量短，避免同义词重复
- 图片文件名尽量和卡片文件名一致
