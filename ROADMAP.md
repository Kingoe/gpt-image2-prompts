# 项目路线图

这个路线图用于把仓库从“可用的提示词收录库”推进到“好逛、好用、可传播的 GPT Image 2 提示词资料库”。

## 当前基线

- 提示词卡片：`60` 条
- 使用场景：`12` 类
- 标签入口：`23` 个
- 已接入效果图预览：`12` 条精选卡片
- 真实生成效果图：`40` 条

## v1.1 内容厚度与首页质感

目标：让仓库更值得收藏，首页更像一个有展示力的灵感库。

执行范围：

- 提示词数量已补到 `60` 条，后续重点转向精选内容打磨和真实效果图补齐。
- 首页精选保持 `12-16` 条，优先展示高传播、高复用卡片。
- 首页精选卡片逐步全部替换成真实生成效果图。
- 建立待配图清单，优先补首页精选和高频场景。
- 新增内容优先覆盖旅行攻略、小红书封面、电商主视觉、品牌/IP、产品展示。

验收标准：

- `npm test` 通过。
- `npm run validate` 通过。
- 首页 30 秒内能看懂项目价值。
- 首页精选区不再大量依赖占位图。
- 已达到 `40` 条真实生成效果图，P0 到 P3 已全部完成。

## v1.2 内容规范升级

目标：降低后续收录成本，让每张卡片更容易复用。

执行范围：

- 为正式卡片增加质量状态，例如 `polished`、`needs-preview`、`experimental`。
- 增加“变量说明”区，统一解释 `{城市名称}`、`{产品名称}`、`{品牌风格}` 等可替换内容。
- 增加“生成注意事项”，沉淀中文小字、地图准确性、人物手部、品牌标识等常见翻车点。
- 更新模板和校验脚本，让新增字段逐步标准化。

验收标准：

- 精选卡片补齐新版字段。
- 新增卡片能明确告诉用户“怎么改、哪里容易错”。
- 校验脚本能识别关键元数据缺失。

## v1.3 真实效果图批量补齐

目标：降低用户试错成本，让仓库从提示词库升级成可预览图库。

执行范围：

- 维护 [preview-requests/backlog.md](preview-requests/backlog.md)。
- 按批次生成真实效果图，并保存到 `assets/previews/`。
- 用 `npm run preview:apply` 回填卡片。
- 不满意的生成图用 `-generated-v2.png` 保留迭代版本，不直接覆盖已有资产。

优先级：

1. 首页精选卡片。
2. 旅行、小红书、电商等传播型内容。
3. 产品展示、UI Mockup、品牌/IP。
4. 其他实验型卡片。

验收标准：

- 至少 `30` 条卡片有真实生成效果图。
- 首页精选全部为 `preview_type: generated`。
- 每张真实图都有清晰来源标注。

## v2.0 轻量展示站

目标：从 GitHub 内容仓库升级为可搜索、可复制、可传播的网站。

推荐方向：

- 使用 `Astro` 做静态内容站。
- Markdown 继续作为唯一内容源。
- 网站自动读取 `library/`、`tags/`、`assets/`。

页面结构：

- 首页：精选卡片瀑布流。
- 场景页：按使用场景浏览。
- 标签页：按视觉效果浏览。
- 卡片详情页：效果图、提示词、一键复制、使用建议、相似推荐。
- 搜索页：按关键词、标签、场景搜索。

验收标准：

- 新增 Markdown 后网站自动更新。
- 不破坏 GitHub 仓库的内容优先结构。
- 卡片页支持一键复制提示词。

## v2.1 社区贡献机制

目标：让外部用户可以低成本投稿，同时不破坏仓库质量。

执行范围：

- 新增 `CONTRIBUTING.md`。
- 新增 Issue 模板：投稿提示词、请求补效果图、标签建议、错误反馈。
- 新增 PR 模板。
- README 增加“如何贡献一条提示词”。
- 明确图片版权规则：外部图不直接搬运，优先自己生成。

验收标准：

- 陌生用户能按模板提交内容。
- 来源和图片版权边界清楚。
- 投稿内容能进入 `inbox -> library` 的现有流程。
