# 项目路线图

这个路线图用于把仓库从“可用的提示词收录库”推进到“好逛、好用、可传播的 GPT Image 2 提示词资料库”。

## 当前基线

- 提示词卡片：`60` 条
- 使用场景：`12` 类
- 标签入口：`113` 个结构化标签，站点首页优先展示高频标签
- 已接入效果图预览：`60` 条正式卡片，其中 README 精选区已统一使用真实生成图
- 真实生成效果图：`60` 条
- 质量状态：`60` 条 `polished`，`0` 条 `needs-preview`
- 新版复用说明：`60` 条卡片已补齐变量说明与生成注意事项，其中首页 `16` 条精选已完成专属精修
- 剩余配图队列：P4 / P5 共 `20` 条已完成，正式卡片达到 `60/60` 真实生成效果图
- v2.0 数据层：已新增 `site-data/`，可从 Markdown 自动导出 prompts / scenes / tags / summary JSON
- v2.0 静态站原型：已新增 `site/`，支持精选浏览、筛选、搜索和复制提示词
- v2.0 自有服务器部署：已支持 `/prompt-atlas/` 路径前缀构建和 `dist/` 输出
- v2.0 上线体验：已支持线上入口、SEO/社交分享信息、场景/标签/搜索状态直链和单张提示词直链
- v2.0 静态索引页：已支持构建 `/scenes/<slug>/` 和 `/tags/<slug>/`，并在站点筛选标签中直接链接
- v2.0 信任说明：已在站点页脚补充来源与版权边界说明
- v2.1 社区贡献机制：已新增 `CONTRIBUTING.md`、Issue 模板和 PR 模板

## v1.1 内容厚度与首页质感

目标：让仓库更值得收藏，首页更像一个有展示力的灵感库。

执行范围：

- 提示词数量已补到 `60` 条，后续重点转向新增内容质量、站点浏览体验和传播路径。
- 首页精选保持 `12-16` 条，优先展示高传播、高复用卡片。
- 首页精选卡片已全部替换成真实生成效果图。
- 待配图清单已完成 `60/60`，后续只在新增卡片时继续维护。
- 新增内容优先覆盖旅行攻略、小红书封面、电商主视觉、品牌/IP、产品展示。

验收标准：

- `npm test` 通过。
- `npm run validate` 通过。
- 首页 30 秒内能看懂项目价值。
- 首页精选区不再依赖占位图。
- 已达到 `60` 条真实生成效果图，全部正式卡片均已覆盖。

## v1.2 内容规范升级

目标：降低后续收录成本，让每张卡片更容易复用。

执行范围：

- 为正式卡片增加质量状态，例如 `polished`、`needs-preview`、`experimental`。`status` 字段已落地到全部正式卡片。
- 增加“变量说明”区，统一解释 `{城市名称}`、`{产品名称}`、`{品牌风格}` 等可替换内容。已补齐到全部正式卡片。
- 增加“生成注意事项”，沉淀中文小字、地图准确性、人物手部、品牌标识等常见翻车点。已补齐到全部正式卡片。
- 更新模板和校验脚本，让新增字段逐步标准化。

验收标准：

- 精选卡片补齐新版字段，首页 `16` 条精选已完成专属变量说明和生成注意事项。
- 新增卡片能明确告诉用户“怎么改、哪里容易错”。
- 校验脚本能识别关键元数据缺失。

## v1.3 真实效果图批量补齐

目标：降低用户试错成本，让仓库从提示词库升级成可预览图库。

执行范围：

- 维护 [preview-requests/backlog.md](preview-requests/backlog.md)。
- 剩余 `20` 张效果图已通过 [preview-requests/remaining-batch.md](preview-requests/remaining-batch.md) 批次补齐。
- 按批次生成真实效果图，并保存到 `assets/previews/`。
- 用 `npm run preview:apply` 回填卡片。
- 不满意的生成图用 `-generated-v2.png` 保留迭代版本，不直接覆盖已有资产。

优先级：

1. 首页精选卡片。
2. 旅行、小红书、电商等传播型内容。
3. 产品展示、UI Mockup、品牌/IP。
4. 其他实验型卡片。

验收标准：

- 至少 `30` 条卡片有真实生成效果图，当前已达到 `60` 条。
- 首页精选全部为 `preview_type: generated`。
- 每张真实图都有清晰来源标注。

## v2.0 轻量展示站

目标：从 GitHub 内容仓库升级为可搜索、可复制、可传播的网站。

推荐方向：

- 使用 `Astro` 做静态内容站。
- Markdown 继续作为唯一内容源。
- 网站自动读取 `library/`、`tags/`、`assets/`。当前已先落地 `site-data/` JSON 导出层，后续页面可直接读取。

页面结构：

- 首页：精选卡片瀑布流。
- 场景页：按使用场景浏览。
- 标签页：按视觉效果浏览。
- 卡片详情页：效果图、提示词、一键复制、使用建议、相似推荐。
- 搜索页：按关键词、标签、场景搜索。

当前静态原型已先把首页瀑布流、场景/标签筛选、关键词搜索和详情复制合并在一个页面内，并已生成轻量的场景页与标签页。后续如果内容继续增多，可再升级成正式多页面 Astro 站。

部署方向：

- 默认部署路径为 `https://kingoecode.com/prompt-atlas/`，避免和 `https://kingoecode.com/ai-daily/` 冲突。
- `npm run build:site` 会输出 `dist/`，用于同步到服务器目录。
- 部署说明见 [DEPLOY.md](DEPLOY.md)。
- 线上链接支持 `?scene=场景slug`、`?tag=标签名`、`?q=关键词` 和 `?prompt=卡片id`，方便从社群、文章和 Issue 中直接分享指定内容。
- 构建产物支持 `/scenes/<slug>/` 和 `/tags/<slug>/`，用于独立分享某个场景或效果标签。

验收标准：

- 新增 Markdown 后网站自动更新。当前已支持 `npm run export:site-data` 重新生成站点数据。
- 不破坏 GitHub 仓库的内容优先结构。
- 卡片页支持一键复制提示词。当前静态原型已支持。
- 单张提示词直链可直接打开详情弹窗。当前静态原型已支持。
- 场景页和标签页可直接打开对应筛选视图。当前静态原型已支持。

## v2.1 社区贡献机制

目标：让外部用户可以低成本投稿，同时不破坏仓库质量。

执行范围：

- 新增 `CONTRIBUTING.md`。已完成。
- 新增 Issue 模板：投稿提示词、请求补效果图、标签建议、错误反馈。已完成。
- 新增 PR 模板。已完成。
- README 增加“如何贡献一条提示词”。已完成。
- 明确图片版权规则：外部图不直接搬运，优先自己生成。已完成。

验收标准：

- 陌生用户能按模板提交内容。
- 来源和图片版权边界清楚。
- 投稿内容能进入 `inbox -> library` 的现有流程。
